namespace WolverineFxContrib.SalesforcePubSub.IntegrationTests;

/// <summary>
/// Placeholder so this project is an obvious, intentional stub. Real integration tests need a live
/// Salesforce org (the sandbox org) + SQL and the at-least-once replay/ack seam, which is not built yet.
/// Replace this with config/secret-gated tests once that work lands.
/// </summary>
public class PlaceholderTests
{
    [Fact(Skip = "Pending: live Salesforce (the sandbox org) + SQL and the at-least-once replay/ack seam.")]
    public void Live_salesforce_round_trip_pending()
    {
    }
}
